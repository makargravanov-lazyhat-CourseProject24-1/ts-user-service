spring:
  application:
    name: ts-user-service
  datasource:
    url: "jdbc:postgresql://${DB_URL}/ts-user-service"
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

server:
  port: ${HTTP_PORT}
